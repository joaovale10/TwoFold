# Vale & Clara — Fase 1 (Base) Implementation Plan

> **Para quem executa:** Sem framework de testes (ver CLAUDE.md). Cada tarefa substitui "correr testes" por `npm run lint`, `npm run build` e verificação manual no browser via `npm run dev`.

**Goal:** Implementar a Fase 1 do `Vale_Clara_estrutura_completa.md` (secção 15): saldo inicial e estado ativa/inativa por conta, transferências entre contas (sem contar como despesa/receita), subcategorias, mantendo a separação pessoal (privada) vs. casal (partilhada) já existente.

**Arquitetura:** Alargar o schema existente (não criar tabelas novas para isto — `accounts`, `categories` e `transactions` ganham colunas). O tipo de movimento `transferencia` é uma transação com `account_id` (origem) + `conta_destino_id` (destino) e sem `categoria_id`; fica fora das somas de receita/despesa mas dentro do saldo de cada conta.

**Tech Stack:** React + Supabase JS (igual ao resto do projeto).

**Spec:** `Vale_Clara_estrutura_completa.md` secções 3 (Transações/Transferências), 4 (Contas), 15 (Roadmap, Fase 1).

## Global Constraints

- Português de Portugal em toda a UI.
- Reutilizar tokens CSS existentes (`--ink`, `--paper`, `--accent-a`, `--accent-b`, `--font-*`) — sem redesign ainda (decidido: redesign só depois da Fase 1).
- Privacidade de contas pessoais mantém-se: uma transferência só pode ter como origem/destino contas a que o utilizador tem acesso (`can_access_account`).
- Commits só se pedidos explicitamente.

---

## Task 1: Schema — saldo inicial, estado, subcategorias, transferências

**Files:**
- Modify: `supabase/schema.sql`

- [ ] **Step 1: Alargar `accounts`**

Na definição de `accounts`, acrescentar antes do `created_at`:

```sql
  saldo_inicial numeric(12, 2) not null default 0,
  ativa boolean not null default true,
```

- [ ] **Step 2: Subcategorias em `categories`**

Acrescentar coluna, depois de `household_id`:

```sql
  parent_id uuid references categories (id),
```

- [ ] **Step 3: Transferências em `transactions`**

Substituir a definição da tabela `transactions` por:

```sql
create table transactions (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households (id) on delete cascade,
  account_id uuid not null references accounts (id) on delete cascade,
  conta_destino_id uuid references accounts (id), -- só preenchido quando tipo = 'transferencia'
  user_id uuid not null references auth.users (id) on delete cascade,
  tipo text not null check (tipo in ('receita', 'despesa', 'transferencia')),
  valor numeric(12, 2) not null check (valor > 0),
  categoria_id uuid references categories (id),
  data date not null default current_date,
  descricao text,
  created_at timestamptz not null default now(),
  constraint transactions_transferencia_valida check (
    (tipo = 'transferencia' and conta_destino_id is not null and conta_destino_id <> account_id and categoria_id is null) or
    (tipo in ('receita', 'despesa') and conta_destino_id is null)
  )
);

create index transactions_household_data_idx on transactions (household_id, data desc);
create index transactions_account_idx on transactions (account_id);
create index transactions_conta_destino_idx on transactions (conta_destino_id);
```

- [ ] **Step 4: Policy de transações cobre também a conta destino**

Substituir a policy `"membros veem transações das contas a que têm acesso"` por:

```sql
create policy "membros veem transações das contas a que têm acesso"
  on transactions for all
  using (
    is_household_member(household_id)
    and can_access_account(account_id)
    and (conta_destino_id is null or can_access_account(conta_destino_id))
  )
  with check (
    is_household_member(household_id)
    and can_access_account(account_id)
    and (conta_destino_id is null or can_access_account(conta_destino_id))
  );
```

- [ ] **Step 5: Verificar**

Como não há dados reais ainda, apagar as tabelas (`drop table transactions, fixed_expenses, budgets, accounts, categories, household_members, households cascade;`) e correr o `schema.sql` completo de novo no SQL Editor do Supabase. Confirmar sem erros.

---

## Task 2: `TransactionForm` — suportar transferências

**Files:**
- Modify: `src/components/TransactionForm.jsx`

**Interfaces:**
- Produces: ao submeter com `tipo === 'transferencia'`, insere `{ ..., conta_destino_id, categoria_id: null }` em vez de `categoria_id`.

- [ ] **Step 1: Estado e toggle com 3 opções**

Adicionar `contaDestinoId` ao estado; o toggle `.tipo-toggle` passa a ter 3 botões (Despesa / Receita / Transferência). Quando `tipo === 'transferencia'`, esconder o `<select>` de categoria e mostrar um `<select>` "Conta destino" com as `accounts` exceto a selecionada como origem.

```jsx
const [contaDestinoId, setContaDestinoId] = useState('')

// no submeter, o insert passa a ser:
const payload =
  tipo === 'transferencia'
    ? {
        household_id: householdId,
        account_id: accountId,
        conta_destino_id: contaDestinoId,
        user_id: userId,
        tipo,
        valor: Number(valor),
        data,
        descricao,
      }
    : {
        household_id: householdId,
        account_id: accountId,
        user_id: userId,
        tipo,
        valor: Number(valor),
        categoria_id: categoriaId || null,
        data,
        descricao,
      }

const { error } = await supabase.from('transactions').insert(payload)
```

No JSX, o botão extra do toggle:

```jsx
<button type="button" className={tipo === 'transferencia' ? 'ativo' : ''} onClick={() => setTipo('transferencia')}>
  Transferência
</button>
```

E a categoria condicional:

```jsx
{tipo === 'transferencia' ? (
  <select value={contaDestinoId} onChange={(e) => setContaDestinoId(e.target.value)} required>
    <option value="">Conta destino</option>
    {accounts.filter((a) => a.id !== accountId).map((a) => (
      <option key={a.id} value={a.id}>{a.nome}</option>
    ))}
  </select>
) : (
  <select value={categoriaId} onChange={(e) => setCategoriaId(e.target.value)}>
    <option value="">Sem categoria</option>
    {categoriasDoTipo.map((c) => (
      <option key={c.id} value={c.id}>{c.nome}</option>
    ))}
  </select>
)}
```

- [ ] **Step 2: Verificar**

`npm run lint`, `npm run build`, depois no browser: criar uma transferência entre duas contas, confirmar que fica gravada e não aparece a somar/subtrair ao "Saldo" da página Transações (que já só soma receita/despesa).

---

## Task 3: `TransactionList` — mostrar transferências

**Files:**
- Modify: `src/components/TransactionList.jsx`

- [ ] **Step 1: Linha neutra + seta para conta destino**

```jsx
<tr key={tx.id} className={tx.tipo}>
  <td>{tx.data}</td>
  <td>
    {tx.descricao || '—'}
    {tx.tipo === 'transferencia' && tx.conta_destino?.nome && ` → ${tx.conta_destino.nome}`}
  </td>
  <td style={{ color: tx.categories?.cor }}>{tx.categories?.nome ?? '—'}</td>
  <td>{tx.accounts?.nome}</td>
  <td>
    {tx.tipo === 'despesa' ? '-' : tx.tipo === 'receita' ? '+' : ''}
    {Number(tx.valor).toFixed(2)} €
  </td>
</tr>
```

Adicionar estilo `tr.transferencia td:last-child { color: var(--muted); }` em `src/index.css`.

- [ ] **Step 2: Ajustar a query em `TransactionsPage.jsx`**

Incluir o nome da conta destino no `select`:

```js
.select('*, categories (nome, cor), accounts (nome, tipo), conta_destino:accounts!transactions_conta_destino_id_fkey (nome)')
```

- [ ] **Step 3: Verificar**

`npm run build`; no browser, confirmar que a transferência aparece com "→ Conta X" e sem sinal +/-.

---

## Task 4: `AccountsPage` — saldo, saldo inicial, ativa/inativa

**Files:**
- Modify: `src/pages/AccountsPage.jsx`

**Interfaces:**
- Consumes: `useOutletContext()` → `{ household, contas, atualizar }`; precisa também das `transactions` do household (nova query própria, tal como `TransactionsPage`).

- [ ] **Step 1: Carregar transações e calcular saldo por conta**

```jsx
const [transacoes, setTransacoes] = useState([])

useEffect(() => {
  supabase
    .from('transactions')
    .select('account_id, conta_destino_id, tipo, valor')
    .eq('household_id', household.id)
    .then(({ data }) => setTransacoes(data ?? []))
}, [household.id])

function saldoDaConta(conta) {
  return transacoes.reduce((saldo, tx) => {
    if (tx.tipo === 'transferencia') {
      if (tx.account_id === conta.id) return saldo - Number(tx.valor)
      if (tx.conta_destino_id === conta.id) return saldo + Number(tx.valor)
      return saldo
    }
    if (tx.account_id !== conta.id) return saldo
    return saldo + (tx.tipo === 'receita' ? Number(tx.valor) : -Number(tx.valor))
  }, Number(conta.saldo_inicial))
}
```

- [ ] **Step 2: Campo "Saldo inicial" no formulário e no insert**

```jsx
const [saldoInicial, setSaldoInicial] = useState('0')

// insert:
const { error } = await supabase.from('accounts').insert({
  household_id: household.id,
  owner_user_id: tipo === 'pessoal' ? user.id : null,
  tipo,
  nome,
  saldo_inicial: Number(saldoInicial) || 0,
})
```

Input no JSX (junto ao `nome`/`tipo`):

```jsx
<input type="number" step="0.01" placeholder="Saldo inicial (€)" value={saldoInicial} onChange={(e) => setSaldoInicial(e.target.value)} />
```

- [ ] **Step 3: Mostrar saldo e alternar ativa/inativa nas listas**

```jsx
async function alternarAtiva(conta) {
  await supabase.from('accounts').update({ ativa: !conta.ativa }).eq('id', conta.id)
  atualizar()
}
```

Substituir os `<li key={c.id}>{c.nome}</li>` por:

```jsx
<li key={c.id} style={{ opacity: c.ativa ? 1 : 0.5 }}>
  {c.nome} — {saldoDaConta(c).toFixed(2)} €{' '}
  <button type="button" className="botao-link" onClick={() => alternarAtiva(c)}>
    {c.ativa ? 'Desativar' : 'Reativar'}
  </button>
</li>
```

- [ ] **Step 4: Verificar**

`npm run lint`, `npm run build`, depois no browser: criar conta com saldo inicial 100€, lançar uma despesa de 20€ nessa conta, confirmar saldo mostrado = 80€. Desativar a conta e confirmar que fica esbatida (continua a aparecer — só deixa de ser sugerida como destino óbvio; a Task 4 não filtra `TransactionForm` por `ativa`, isso fica para uma iteração seguinte se fizer falta).

---

## Task 5: `CategoriesPage` — subcategorias

**Files:**
- Modify: `src/pages/CategoriesPage.jsx`

- [ ] **Step 1: Selecionar categoria-mãe ao criar**

```jsx
const [parentId, setParentId] = useState('')

// insert:
const { error } = await supabase
  .from('categories')
  .insert({ household_id: household.id, nome, tipo, cor, parent_id: parentId || null })
```

Select no JSX, só com categorias do mesmo `tipo` sem `parent_id` (não permitir sub-subcategorias):

```jsx
<select value={parentId} onChange={(e) => setParentId(e.target.value)}>
  <option value="">Categoria principal (sem categoria-mãe)</option>
  {categorias.filter((c) => c.tipo === tipo && !c.parent_id).map((c) => (
    <option key={c.id} value={c.id}>{c.nome}</option>
  ))}
</select>
```

- [ ] **Step 2: Mostrar como árvore**

```jsx
const principais = proprias.filter((c) => !c.parent_id)
const subDe = (paiId) => proprias.filter((c) => c.parent_id === paiId)
```

```jsx
<ul className="categoria-lista">
  {principais.map((c) => (
    <li key={c.id}>
      <span className="categoria-cor" style={{ background: c.cor }} /> {c.nome}
      {subDe(c.id).length > 0 && (
        <ul className="categoria-lista categoria-lista--sub">
          {subDe(c.id).map((sub) => (
            <li key={sub.id}>{sub.nome}</li>
          ))}
        </ul>
      )}
    </li>
  ))}
</ul>
```

Estilo extra em `src/index.css`: `.categoria-lista--sub { margin-top: 0.35rem; margin-left: 0.5rem; }`.

- [ ] **Step 3: Verificar**

`npm run build`; no browser, criar categoria "Supermercado" com categoria-mãe "Alimentação", confirmar que aparece indentada por baixo.

---

## Self-Review

- **Cobertura da Fase 1 (secção 15 do spec):** BD ✅ (Task 1) · Contas com saldo/estado ✅ (Task 4) · Transações ✅ (já existia) · Transferências ✅ (Tasks 1-3) · Categorias/Subcategorias ✅ (Task 5) · Separação pessoal vs. casal ✅ (já implementado na sessão anterior, `can_access_account`).
- **Placeholders:** nenhum.
- **Consistência de tipos:** `conta_destino_id` usado com o mesmo nome no schema (Task 1), `TransactionForm` (Task 2), `TransactionList`/`TransactionsPage` (Task 3) e `AccountsPage` (Task 4).
